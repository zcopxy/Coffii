import SwiftUI

/// Espresso iconography. SF Symbols where they fit cleanly; custom SVG art is
/// deferred (per spec) — these cover Phase 0–3 needs.
enum EspressoIcon {
    static let cup          = "cup.and.saucer.fill"
    static let bag          = "bag.fill"
    static let timer        = "timer"
    static let thermometer  = "thermometer"
    static let scale        = "scalemass.fill"
    static let droplet      = "drop.fill"
    static let share        = "square.and.arrow.up"
    static let chart        = "chart.line.uptrend"
    static let star         = "star.fill"
    static let bean         = "circle.grid.2x1.fill"
}
